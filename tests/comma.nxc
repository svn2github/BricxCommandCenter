task main()
{
  int x, y, z, w;
  y = x++, z--, w/4; // 2011-07-18 - code does not compile (but same as previous release)
  NumOut(0, LCD_LINE1, y);
  for (int i=0; y++, y > 23; w++, i++)
  {

  }
}
