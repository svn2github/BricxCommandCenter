task main()
{
  int x;
  bool b;
  char data[] = {1, 2, 3, 4, 5, 6};

  printf("sizeof(x) = %d\n", SizeOf(x));
  printf("sizeof(b) = %d\n", SizeOf(b));
  printf("sizeof(data) = %d\n", SizeOf(data));
  while(true);
}
