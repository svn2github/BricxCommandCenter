// test.nxc

int array[8];

task main()
{
//   array = 42;
//   asm { mov array, 42 }
   array += 42;
   for (int i = 0; i < 8; ++i)
   {
      NumOut(0, 56 - 8 * i, array[i]);
   }
   while (true);
}
