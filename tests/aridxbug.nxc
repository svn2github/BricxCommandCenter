//Initial Comment:
// space (blank) between array name and index causes error

float tOutCribSheet[10];  // crib sheet for the auto teach agent
float targOut;
task main()
{
  int j;
  targOut = tOutCribSheet [j] ;  // space (blank) between array name and index causes error
  NumOut(0, LCD_LINE1, targOut);
  Wait(SEC_1);
}
