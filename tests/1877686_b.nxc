// fixed
task main()
{
  int x1=10, y1=10, x2=40, y2=40;
  LineOut(x1, y1, x2, y2);
  LineOut(x1, y1+10, x2, y2+10); // 10 is added to y
  LineOut(x1+10, y1, x2+10, y2); // 10 not added to x
  while(true);
}

