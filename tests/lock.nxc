task main(void)
{
  float x = 1;
  float y = 55;
  float z =3;

  z += sign(x)*y;

  NumOut(0, LCD_LINE1, x);
  NumOut(0, LCD_LINE2, y);
  NumOut(0, LCD_LINE3, z);
  TextOut(0, LCD_LINE4, "done");
  Wait(SEC_5);
}
